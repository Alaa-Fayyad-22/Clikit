/**
 * IMPORTANT: replace with your real production domain before launch.
 * Used for the sitemap, robots.txt, canonical URLs, and social share
 * previews — leaving this as-is will point all of those at a placeholder.
 *
 * Can be overridden at build time via NEXT_PUBLIC_SITE_URL (see .env.example)
 * so the domain isn't hardcoded across environments. This is a public URL, so
 * the NEXT_PUBLIC_ prefix is intentional and safe.
 */
export const SITE_URL =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.clikit.agency";

export const SITE_NAME = "CLiKiT";
